extends ../shared/body.pug

block title
  | CANCELLATION OF RENTAL

block content
  p
    | We regret to inform you that your booking from #{dayjs(booking.start).format('LL')} to #{dayjs(booking.end).format('LL')} has been canceled.
    br
