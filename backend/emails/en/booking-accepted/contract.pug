doctype 5
html
    head
        style(type="text/css").
            h1 {
                text-align: center;
            }
            .underline {
                text-decoration: underline;
            }
            .right {
                text-align: right;
            }
            .container {
                margin: auto;
            }


div.container
    h1 SEASONAL RENTING CONTRACT

    table(style='width:100%')
        tr
            td Last name: #{landlord.lastName}
            td.right First name: #{landlord.firstName}


    p Address: #{landlord.address}
    p Phone number: #{landlord.phone}

    p.underline Identified as the lessor on one hand


    table(style='width:100%')
        tr
            td Last name: #{booking.customer.lastName}
            td.right First name: #{booking.customer.firstName}
        tr
            td Phone number: #{booking.customer.phone}
            td.right Email: #{booking.customer.email}
        tr
            td Adress: #{booking.customer.address.line1} #{booking.customer.address.line2}, #{booking.customer.address.postalCode} #{booking.customer.address.city}, #{booking.customer.address.country}


    p.underline Named the lessee on the other hand

    p It has been agreed to a seasonal rental for the period
        ul
            li from: #{dayjs(booking.start).format('DD/MM/YYYY')}
            li to: #{dayjs(booking.end).format('DD/MM/YYYY')}
        
    p Renting house address #{renting_address}

    p Total rent price (charges included): #{booking.totalPrice} euros

    // p The deposit is 30% of the booking so 180 euros

    p The balance of 400 euros as well as a security deposit of: 200 euros must be paid on the day when you get the keys, i.e. #{dayjs(booking.start).format('LL')}

    p Arrival time is normally scheduled for Saturday afternoon at 4 p.m.

    p The departure time is normally scheduled for Saturday at 10am.

    p Made in two copies at SAINT CHAMASSY the #{dayjs().format('LL')}


    table(style='width:100%')
        tr
            td The lessor
            td.right The tenant
        tr
            td #{landlord.lastName} #{landlord.firstName}
            td.right read and approved
